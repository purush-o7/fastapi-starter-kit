import type { Metadata } from "next";
export const metadata: Metadata = { title: "HTTP Exceptions", description: "Raise structured error responses with appropriate HTTP status codes in FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
