import type { Metadata } from "next";
export const metadata: Metadata = { title: "Custom Handlers", description: "Register custom exception handlers for consistent error formatting in FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
