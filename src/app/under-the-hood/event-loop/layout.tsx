import type { Metadata } from "next";
export const metadata: Metadata = { title: "The Event Loop", description: "How Python's asyncio event loop handles concurrent requests in FastAPI without threads." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
