import type { Metadata } from "next";
export const metadata: Metadata = { title: "ASGI vs WSGI", description: "The two Python server standards — why FastAPI uses ASGI and what that means for performance." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
