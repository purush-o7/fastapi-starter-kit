import type { Metadata } from "next";
export const metadata: Metadata = { title: "APIRouter", description: "Organize endpoints into modular groups with shared prefixes and tags in FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
