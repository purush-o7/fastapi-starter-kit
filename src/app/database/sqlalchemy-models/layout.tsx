import type { Metadata } from "next";
export const metadata: Metadata = { title: "SQLAlchemy Models", description: "Define database tables, column types, and relationships using SQLAlchemy ORM." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
