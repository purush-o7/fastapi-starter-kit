import type { Metadata } from "next";
export const metadata: Metadata = { title: "CRUD Operations", description: "Repository pattern for create, read, update, delete with SQLAlchemy and FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
