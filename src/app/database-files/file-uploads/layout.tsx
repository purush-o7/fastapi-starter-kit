import type { Metadata } from "next";
export const metadata: Metadata = { title: "File Uploads", description: "Handle single and multiple file uploads with validation and storage in FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
