import type { Metadata } from "next";
export const metadata: Metadata = { title: "OAuth2 & JWT", description: "Implement token-based authentication with OAuth2 password flow and JWT tokens in FastAPI." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
