import type { Metadata } from "next";
export const metadata: Metadata = { title: "Async Endpoints", description: "Use async/await for non-blocking I/O in your FastAPI path operations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
